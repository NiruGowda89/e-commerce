# Server Configuration
server.port=${PORT:8080}



# Database (PostgreSQL on Render)
spring.datasource.url=jdbc:postgresql://CBBcbjJopg5dgYZPAU59vxEzbPRjHXdt@dpg-d8lvkr6gvqtc73cg8i5g-a:5432/karunada_db
spring.datasource.username=karunada_db_user
spring.datasource.password=CBBcbjJopg5dgYZPAU59vxEzbPRjHXdt
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect


# Email
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Razorpay
razorpay.key.id=${RAZORPAY_KEY_ID:}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:}

# Logging
logging.level.org.springframework=INFO
logging.level.com.urbanman=DEBUG
